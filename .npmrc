auto-install-peers=true
strict-peer-dependencies=false

# @dragonpass/* (dp-design) resolves from the private registry; everything else
# stays on the default public npm registry. Pulling these requires the corp network/VPN.
@dragonpass:registry=http://npm.dragonpass.com.cn
